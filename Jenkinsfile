pipeline {
    agent any
    stages {
        stage('Docker Image') {
            steps {
                 script {
                      sh "bash docker build -t test ."
                 }
            }
        }


        }
    }
